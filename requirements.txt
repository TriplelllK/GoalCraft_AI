# ── Core ─────────────────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
httpx>=0.27,<1.0

# ── LLM (optional — graceful degradation without key) ────────────────
openai>=1.40,<3.0

# ── Storage backends (optional — defaults to in-memory) ──────────────
psycopg2-binary>=2.9,<3.0
qdrant-client>=1.9,<2.0
numpy>=1.24,<3.0

# ── Testing ──────────────────────────────────────────────────────────
requests>=2.31,<3.0
