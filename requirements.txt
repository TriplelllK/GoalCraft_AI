# ── GoalCraft AI — Python Dependencies ───────────────────────────
# pip install -r requirements.txt

# ── Core Framework ───────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# ── LLM (optional — graceful degradation without API key) ────────
openai>=1.40,<3.0

# ── Storage Backends (optional — defaults to in-memory) ──────────
psycopg[binary]>=3.1,<4.0          # PostgreSQL adapter (async-ready, v3)
psycopg2-binary>=2.9,<3.0          # PostgreSQL adapter (legacy fallback)
qdrant-client>=1.9,<2.0            # Qdrant vector database client
numpy>=1.24,<3.0                   # Vector operations for RAG

# ── Testing ──────────────────────────────────────────────────────
requests>=2.31,<3.0                # HTTP client for test suites
pytest>=8.0,<10.0                  # CI test runner
httpx>=0.27,<1.0                   # async HTTP client (FastAPI TestClient)
